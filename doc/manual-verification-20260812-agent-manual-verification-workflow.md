# Agent 人工验证工作流验证记录

## 基本信息

- 验证状态：待验证
- 待验证变更：`AGENTS.md` 新增“Manual application verification”规则
- 目标平台：Windows 与 macOS（本次规则为平台无关变更，可先在当前平台验证）
- 验证方式：用户手动验证
- 验证人：待填写
- 验证日期：待填写

## 变更说明

本次变更要求 Codex 在用户未明确提出使用 computer use 时，不得通过 computer use、GUI 自动化或模拟鼠标键盘操作来验证应用。若改动需要交互式验证，Codex 应在 `doc/` 下创建中文人工验证文档，由用户执行验证；用户反馈完成后，Codex 再记录结果并将文档归档到 `doc/archive/`。

命令行测试、集成测试和生产构建不受该限制，仍应由 Codex 按任务需要执行。

## 验证前提

1. 使用包含本次 `AGENTS.md` 变更的工作区或提交。
2. 当前任务没有明确要求 Codex 使用 computer use。
3. 准备一个确实需要启动桌面应用、点击界面或检查窗口行为的测试改动。

## 验证步骤

### 1. 检查规则内容

操作：打开项目根目录的 `AGENTS.md`，检查“Manual application verification”章节。

预期结果：文档明确规定，只有用户在当前任务中明确要求时才能使用 computer use、GUI 自动化或模拟鼠标键盘输入。

实际结果：


### 2. 检查非 GUI 验证行为

操作：让 Codex 处理一个需要运行测试和生产构建的改动，但不要要求使用 computer use。

预期结果：Codex 可以正常运行适用的命令行测试、集成测试和生产构建，不会把规则误解为禁止所有自动验证。

实际结果：


### 3. 检查交互验证交接

操作：让 Codex 处理一个需要启动桌面应用并检查界面、窗口、快捷键或文件对话框的改动，但不要要求使用 computer use。

预期结果：Codex 不会自动操作应用，而是在 `doc/` 下创建名称符合 `manual-verification-YYYYMMDD-<topic>.md` 格式的人工验证文档，并明确告知用户文档路径。

实际结果：


### 4. 检查人工验证文档内容

操作：打开上一步生成的人工验证文档。

预期结果：文档使用中文，包含待验证变更或提交、目标操作系统、验证前提、编号步骤、每一步的预期结果，以及留空的实际结果、总体结果和发现问题字段。

实际结果：


### 5. 检查归档流程

操作：完成验证后，将验证结果告知 Codex，并要求其处理验证记录。

预期结果：Codex 将用户反馈写入验证文档，并把文档移动到 `doc/archive/`；除非发生重名冲突，否则保留原文件名。

实际结果：


## 总体结果

- 结论（通过/不通过）：待填写
- 已发现问题：待填写
- 补充说明：待填写
