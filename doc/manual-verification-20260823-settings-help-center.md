# 设置页与内置帮助中心人工验证

## 验证对象

- 变更：重新设计设置页左侧导航和内容节奏，新增随应用版本提供的本地帮助与反馈中心，并将“Tag 缓存”用户界面名称调整为“Tag 数据”
- 相关提交：`c9555f9`、`0517047`、`dd616c3`
- 目标操作系统：Windows 11、macOS
- 自动检查：干净 `npm ci` 成功；`npm test` 通过 44 个测试文件、263 项测试；`npm run build` 成功；Lobe UI 5.20.3 审计为 0 个问题

## 前置条件

- 使用包含提交 `dd616c3` 的开发版本或安装包。
- 准备至少一张可读取 Prompt 的 NovelAI 图片，并在图库中保留少量可操作图片。
- 网络可用，以便验证 GitHub 反馈入口；帮助文章本身不依赖网络。
- 测试期间不要在打开的 GitHub 页面实际提交问题或建议，除非确实需要反馈。

## 验证步骤

1. 在宽窗口中打开设置页，观察展开的左侧导航并依次选择所有入口。
   - 预期结果：图标和文字左对齐；“偏好”“数据与服务”“支持”分组清楚；帮助与关于入口位于支持区域；当前项使用完整低对比度选中表面，不出现居中的图标文字或单侧强调条；切换页面时内容不跳位。
   - 实际结果：Windows 初次检查发现“Tag 数据”中的小写 `g` 下行部被裁切；已在 `0517047` 中增加导航文字的明确行高与垂直留白，待复验。

2. 检查外观、资源库、AI 服务、Tag 数据和关于与更新页面的现有功能。
   - 预期结果：主题、主题色、字体、动效、资源位置、AI 配置、Tag 数据管理和更新检查能力保持可用；“Tag 数据”页面的标题、搜索标签、统计和空状态术语一致；设置内容区在宽窗口中比旧版更充分利用空间。
   - 实际结果：

3. 打开“帮助与反馈”，搜索“Prompt”“框选”“快捷键”和一个不存在的词，再清空搜索。
   - 预期结果：帮助内容无需网络即可显示；搜索结果立即按任务过滤；选择结果后右侧文章正确切换；无结果状态提供可理解的搜索建议；清空后恢复全部六个主题。
   - 实际结果：

4. 依次阅读快速开始、Prompt 范围、选择与批量操作、图片库、快捷操作、数据与隐私六篇文章，并操作 Prompt 范围演示。
   - 预期结果：文章与当前版本功能和术语一致；Prompt / Undesired 与 Base / Character 演示可独立切换；演示只使用固定数据，不修改工作台、图库、设置或本地文件；页面不加载截图、视频或在线文档。
   - 实际结果：Windows 初次检查发现帮助文章标题图标视觉上偏左上；`0517047` 将图标相对标题与摘要整体垂直居中，但复验发现图标因此落到主标题下方，未通过。`dd616c3` 改为让图标锚定主标题行并保留水平光学校正，待再次复验。

5. 将动效分别切换为“完整”“跟随系统”和“关闭”，查看快速开始、框选和图库选择演示。
   - 预期结果：完整动效下 Tag 焦点、框选和图库选择演示平滑且不干扰阅读；系统减少动态效果生效时以及关闭动效时，演示变为清楚的静态状态，不快速闪烁、不持续占用性能，文字仍能独立说明操作。
   - 实际结果：

6. 分别切换浅色、深色、跟随系统主题和至少两个主题色，并更换界面字体。
   - 预期结果：设置导航、帮助文章、Tag、键帽、演示表面和反馈区随真实应用主题立即更新；深浅主题的信息层级等价；没有额外主题模拟预览、固定颜色、发光或玻璃效果。
   - 实际结果：

7. 在 100% 与 125% 系统缩放下测试宽窗口和窄窗口，并用键盘遍历设置导航、帮助搜索、主题列表和反馈按钮。
   - 预期结果：窄窗口下设置导航按现有侧栏行为收起，帮助主题和文章改为纵向布局；文字、演示和操作没有横向裁切；焦点清晰可见；当前页面和帮助主题能由辅助技术识别。
   - 实际结果：

8. 点击“复制诊断信息”，检查剪贴板内容；分别点击“报告问题”和“提出建议”。
   - 预期结果：诊断信息只包含应用版本和平台，不包含图片路径、Prompt、API 地址、API Key 或日志；两个反馈入口只在系统浏览器中打开官方 `LBEILC/NovelAIPromptStudio` GitHub 新问题页面，并分别预填问题或建议标题，不会自动提交内容。
   - 实际结果：

9. 从设置页返回工作台和图库，再重新进入设置页。
   - 预期结果：工作台和图库状态保持不变；设置页能够重新进入；帮助演示没有留下计时器、焦点、选择或业务数据副作用。
   - 实际结果：

## 验证结论

- 总体结果：Windows 初次视觉检查发现 2 项细节问题；第二次检查确认帮助标题图标的首次修正未通过，已按主标题行重新对齐并通过自动检查，等待再次人工复验；其余步骤尚未完成。
- 发现的问题：“Tag 数据”小写 `g` 下行部被裁切，由 `0517047` 修复，待复验；帮助文章标题图标位置异常，`0517047` 的首次修正未通过，已由 `dd616c3` 改为主标题行对齐，待再次复验。
- 验证人：用户（Windows，初次视觉检查）
- 验证日期：2026-08-24
