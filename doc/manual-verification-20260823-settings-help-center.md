# 设置页与内置帮助中心人工验证

## 验证对象

- 变更：重新设计设置页左侧导航和内容节奏，新增随应用版本提供的本地帮助与反馈中心，将“Tag 缓存”用户界面名称调整为“Tag 数据”，并把复制范围、草稿、原文保真、选择与批量操作、图库分组/筛选/作用域/回收站、数据流、排错和诊断边界改为交互组件说明
- 相关提交：`c9555f9`、`0517047`、`dd616c3`、`bcd7d0a`、`df79614`、`87c1780`、`4f916ca`、`80f269b`、`b1ecc6e`、`7fe7ad3`、`95a6c14`、`19c27fc`
- 目标操作系统：Windows 11、macOS
- 自动检查：独立干净工作区 `npm ci` 成功；`npm test` 通过 47 个测试文件、278 项测试；`npm run build` 成功；Lobe UI 5.20.3 审计为 0 个问题

## 前置条件

- 使用包含提交 `19c27fc` 的开发版本或安装包。
- 准备至少一张可读取 Prompt 的 NovelAI 图片，并在图库中保留少量可操作图片。
- 网络可用，以便验证 GitHub 反馈入口；帮助文章本身不依赖网络。
- 测试期间不要在打开的 GitHub 页面实际提交问题或建议，除非确实需要反馈。

## 验证步骤

1. 在宽窗口中打开设置页，观察展开的左侧导航并依次选择所有入口。
   - 预期结果：图标和文字左对齐；“偏好”“数据与服务”“支持”分组清楚；帮助与关于入口位于支持区域；当前项使用完整低对比度选中表面，不出现居中的图标文字或单侧强调条；切换页面时内容不跳位。
   - 实际结果：Windows 初次检查发现“Tag 数据”中的小写 `g` 下行部被裁切；已在 `0517047` 中增加导航文字的明确行高与垂直留白，待复验。

2. 检查外观、资源库、AI 服务、Tag 数据和关于与更新页面的现有功能。
   - 预期结果：主题、主题色、字体、动效、资源位置、AI 配置、Tag 数据管理和更新检查能力保持可用；“Tag 数据”页面的标题、搜索标签、统计和空状态术语一致；设置内容区在宽窗口中比旧版更充分利用空间。
   - 实际结果：

3. 打开“帮助与反馈”，搜索“Prompt”“框选”“快捷键”“来源不可用”“API Key”“智能收藏集”“DSO”“未分类”“按分类”和一个不存在的词，再清空搜索。
   - 预期结果：帮助内容无需网络即可显示；搜索结果立即跨分组按任务、排错、隐私、翻译和分类术语过滤，匹配分组自动展开；“DSO”“未分类”和“按分类”均能找到“翻译与分类 Tag”，“压缩包”和“相似 Prompt”分别找到导入与分组文章；选择结果后右侧文章正确切换；无结果状态提供可理解的搜索建议；清空后恢复“开始使用”“工作台”“图片库”“参考与支持”四组共十七篇文章。
   - 实际结果：

4. 展开四个帮助分组，依次阅读全部十七篇文章；操作工作台相关的 Prompt 范围、复制范围、翻译分类、原文保真、选择与批量操作及标签草稿六组演示。
   - 预期结果：当前四组各有四篇，但分组数据和测试不限制文章上限，未来可以继续扩充。Prompt / Undesired 与 Base / Character 可以独立切换；复制演示中的内容、作用域和分类共同决定当前可见结果，Undesired 不会混入正向复制；翻译分类演示可切换结构/分类、原文/翻译/对照、分类筛选及整理前后状态，并呈现用户修正、Danbooru 画师验证、DSO、本地规则和可选 AI 的顺序；原文保真演示可比较结构视图与原始文本并显示顺序、权重和 `Text:` 保留；手动增减选择后框选示意收起，复制/翻译/分类/移动/删除只说明当前选中数量；标签演示可分别显示多图、草稿、重启恢复和来源失效状态。
   - 实际结果：Windows 初次检查发现帮助文章标题图标视觉上偏左上。`0517047` 将图标相对标题与摘要整体居中，复验发现图标落到主标题下方；`dd616c3` 改为顶部偏移，复验仍发现图标相对标题行偏上。两次修正均未通过。`bcd7d0a` 将图标与 `h3` 放入同一 Grid 行并垂直居中，摘要独立放在第二行。后续检查又发现框选演示与提示块紧贴，以及“数据与隐私”顶部出现重复分隔线；`df79614` 增加 12px 区块间距并移除列表首行的重复上边框。用户认为原六篇内容过于简略；`87c1780` 已重组为四组十五篇任务文章，并补充完整步骤、操作边界、排错和反馈说明。用户随后发现工作台仍缺少分类视图和翻译与分类功能介绍；`4f916ca` 新增独立文章，并将帮助中心扩充为四组十六篇。后续评审认为每组四篇不应成为未来上限，且翻译分类更适合组件说明；`80f269b` 移除上限假设并将该文章改为交互演示，待复验。

5. 操作图片库和支持相关的导入、自动分组、组合筛选、图片组作用域、回收站生命周期、数据流、常见问题和诊断边界演示。
   - 预期结果：导入与分组是两篇独立文章；导入演示可在单图和 NovelAI ZIP 之间切换，ZIP 会展开多张图片，并展示读取、去重、保存应用副本和加入图库的顺序。“模拟导入”按钮与左侧分段控件均为 32px 高，视觉中心和上下边缘对齐。分组演示使用与真实图库一致的工具栏、设置面板、卡片堆叠和数量标记，四种方式会直接合并或拆分卡片，相似模式才显示阈值，跨 Vibe 开关具有与实际功能相同的禁用边界。筛选演示使用与真实图库一致的包含/排除 Tag、全部/任意、模型、Vibe、日期和智能收藏集控件，右侧直接显示过滤后的图库卡片；在图片组卡片上横向移动指针时，预览会按实际卡片宽度切换三个成员，左右方向键也能逐张预览，单击后详情侧栏固定到当时的单个成员；切到批量工具栏后则明确显示整组 3 张均被选中。回收站演示允许恢复，永久删除必须二次确认；其余数据流、排错和诊断边界与实际功能一致。
   - 实际结果：

6. 分别切换浅色、深色、跟随系统主题和至少两个主题色，并更换界面字体。
   - 预期结果：设置导航、帮助文章、Tag、键帽、翻译分类组件、演示表面和反馈区随真实应用主题立即更新；分类 Tag 使用应用已有的分类颜色变量；深浅主题的信息层级等价；没有额外主题模拟预览、固定颜色、发光或玻璃效果。
   - 实际结果：

7. 在 100% 与 125% 系统缩放下测试宽窗口和窄窗口，并用键盘遍历设置导航、帮助分组、帮助搜索、十二组交互演示、主题列表和反馈按钮。
   - 预期结果：四个原生帮助分组可由键盘展开和收起，当前文章所在组默认展开；窄窗口下设置导航按现有侧栏行为收起，帮助主题和文章改为纵向布局，演示按钮在需要时占满可用宽度，图库图片网格由四列降为两列或单列，数据流改为纵向，五步翻译来源顺序改为单列；比较表只在自身区域横向滚动，其他文字和操作没有裁切；焦点清晰，分段控件、开关、筛选按钮和图片组擦看卡片的当前状态能由辅助技术识别。
   - 实际结果：

8. 点击“复制诊断信息”，检查剪贴板内容；分别点击“报告问题”和“提出建议”。
   - 预期结果：诊断信息只包含应用版本和平台，不包含图片路径、Prompt、API 地址、API Key 或日志；两个反馈入口只在系统浏览器中打开官方 `LBEILC/NovelAIPromptStudio` GitHub 新问题页面，并分别预填问题或建议标题，不会自动提交内容。
   - 实际结果：

9. 从设置页返回工作台和图库，再重新进入设置页。
   - 预期结果：工作台和图库状态保持不变；设置页能够重新进入；帮助演示没有留下计时器、焦点、选择或业务数据副作用。
   - 实际结果：

## 验证结论

- 总体结果：Windows 多轮视觉检查发现的细节问题、内容深度、缺失主题和长文字说明问题均已有对应修正；`b1ecc6e` 完成 P1–P3 组件化说明，`7fe7ad3` 进一步拆开图库导入与分组，`95a6c14` 统一导入演示中分段控件与操作按钮的 32px 控件高度，`19c27fc` 用真实横向命中逻辑重做图片组擦看、单图详情和整组批量范围演示，已通过自动检查，等待人工复验。
- 发现的问题：“Tag 数据”小写 `g` 下行部被裁切，由 `0517047` 修复；帮助文章标题图标位置异常，由 `bcd7d0a` 修复；间距和重复分隔线由 `df79614` 修复；文章深度、翻译分类和增长上限由 `87c1780`、`4f916ca`、`80f269b` 修正；其余说明由 `b1ecc6e` 组件化。用户复核后认为图库导入与分组不应合并，且分组与筛选演示和实际软件差异过大；`7fe7ad3` 将其拆为十七篇，并用实际图库视觉结构重做三组演示。之后“组内浏览与批量范围”仍被认为不直观，`19c27fc` 增加与真实图库一致的指针位置预览、点击打开当前成员和整组批量状态。全部待复验。
- 验证人：用户（Windows，初次视觉检查）
- 验证日期：2026-08-24
