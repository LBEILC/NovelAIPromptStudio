# 工作台胶片条间距验证

## 验证对象

- 变更：将工作台缩略图胶片格之间的间距从 7 像素收紧为 4 像素
- 相关提交：`226ee51`
- 目标操作系统：Windows 11
- 自动检查：`npm test` 通过 41 个测试文件、254 项测试；`npm run build` 成功；Lobe UI 5.20.3 审计为 0 个问题

## 前置条件

- 使用包含提交 `226ee51` 的开发版本，或由验证人自行构建并安装的新版本。
- 在工作台打开至少 12 张图片，并保留普通、当前和未保存修改等不同标签状态。

## 验证步骤

1. 先查看只有 3 个标签的胶片条，再打开至少 12 个标签。
   - 预期结果：相邻胶片格保持紧凑且仍可清楚区分，视觉上形成连续胶片；格间距为 4 像素，不再显得松散。
   - 实际结果：

2. 缩窄窗口触发胶片条溢出，并切换首个、中间和最后一个标签。
   - 预期结果：收紧间距后仍保持单行，前后导航、当前项自动显露和无原生横向滚动条的行为不变。
   - 实际结果：

3. 检查普通、当前、悬停、未保存修改、关闭按钮和拖动排序状态。
   - 预期结果：相邻标签不会因间距收紧而发生边框、预览、徽标或点击区域重叠，拖动落点仍清晰。
   - 实际结果：

4. 在浅色和深色主题下检查同一组标签。
   - 预期结果：两种主题的胶片格边界都能辨认，整体密度紧凑但不拥挤。
   - 实际结果：

## 验证结论

- 总体结果：
- 发现的问题：
- 验证人：
- 验证日期：
