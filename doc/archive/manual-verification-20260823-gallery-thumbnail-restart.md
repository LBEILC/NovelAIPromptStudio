# 图库缩略图重启恢复手动验证

## 验证对象

- 变更提交：`3600610`（修复从图库打开到工作台后，重启应用导致图库缩略图保持透明的问题）
- 目标操作系统：Windows 11；macOS 当前受支持版本
- 自动检查：干净 `npm ci` 成功；`npm test` 通过 253 项测试；`npm run build` 成功

## 前置条件

- 使用包含提交 `3600610` 的开发版或打包版本。
- 图库中至少有六张图片，其中至少一组包含多张成员图片。
- 工作台会话恢复功能可正常使用。

## 验证步骤

1. 启动应用并进入图库，确认首屏普通图片和图片组的封面、堆叠缩略图均正常显示。
   - 预期结果：缩略图正常完成淡入，没有持续空白或只有背景色的卡片。
   - 实际结果：

2. 从图库中分别打开两张图片到工作台，停留在工作台页面，然后完整退出应用。
   - 预期结果：两个工作台标签均正常打开；退出前图库数据与源图片不受影响。
   - 实际结果：

3. 重新启动应用，等待工作台标签恢复，然后直接进入图库，不要先切换到回收站。
   - 预期结果：图库首屏所有缩略图立即正常显示；此前打开到工作台的两张图片不会变成透明空白卡片。
   - 实际结果：

4. 在图库中向下滚动，使延迟渲染的后续图片进入可视区域，再回到工作台并重新进入图库。
   - 预期结果：后续缩略图在进入可视区域时正常加载；页面往返不会让已加载缩略图重新变成空白。
   - 实际结果：

5. 再次完整退出并重启应用，直接进入图库，然后切换一次回收站并返回全部图片。
   - 预期结果：进入图库时缩略图已经正常显示；切换回收站前后显示结果一致，不再需要通过切换视图修复空白。
   - 实际结果：

6. 分别在浅色和深色主题下重复步骤 2 至步骤 3。
   - 预期结果：两种主题下缩略图均正常显示，加载背景和淡入效果保持一致。
   - 实际结果：

## 验证结论

- 总体结果：Windows 验证通过；macOS 对应检查继续由 `coordination/MACOS.md` 跟踪。
- 发现的问题：Windows 本轮未报告问题。
- 验证人：用户
- 验证日期：2026-08-24
