# Demo Profile 人工验证清单

- 变更或提交：`1621812 feat: add isolated app profiles`
- 目标操作系统：Windows 10/11
- 验证状态：Windows 人工验证通过

## 前置条件

- 已关闭所有 NovelAI Prompt Studio 实例。
- 当前默认环境已经存在至少一张可识别的日常图片；记录其名称，但不要在本文写入私人绝对路径。
- `demo/promo-v090/final/` 中存在 14 张演示图片。
- 已在仓库根目录执行本变更要求的自动化检查与生产构建。

## 自动化检查

- Windows `npm ci`：通过；仅报告现有 peer dependency、弃用包和 install-script 审批警告。
- Windows `npm test`：通过，43 个测试文件、261 项测试全部成功。
- Windows `npm run build`：通过；仅报告现有的大 Chunk 提示。
- GUI 自动化：宣传取图阶段经用户授权执行；不替代本清单中由用户完成的环境隔离验证。

## 验证步骤

1. 在仓库根目录运行 `npm start`，进入图片库并记录默认环境中可见图片数量和一张日常图片名称，然后完全退出应用。
   - 预期结果：默认环境正常打开，原有图库、收藏集和设置均存在，没有发生迁移或清空。
   - 实际结果：默认环境正常，原有数据未受影响。

2. 运行 `npm run start:demo`，进入图片库和设置页。
   - 预期结果：首次启动的 Demo Profile 是空图库；默认环境的收藏集、API Key、最近目录和工作台标签没有出现。
   - 实际结果：Demo Profile 首次进入为空，未出现默认环境的数据或设置。

3. 在 Demo Profile 中导入 `demo/promo-v090/final/` 的 14 张图片。
   - 预期结果：14 张图片成功进入演示图库，名称和 NovelAI 元数据可读；默认环境中的日常图片没有混入。
   - 实际结果：14 张演示图片全部导入，名称与 NovelAI 元数据可读，未混入日常图库。

4. 在 Demo Profile 中创建名为“本周精选”的手动收藏集，加入至少两张演示图片；调整一次主题或图库缩略图尺寸，然后完全退出应用。
   - 预期结果：收藏集、成员关系和界面设置保存成功，应用退出时没有数据库或资源目录错误。
   - 实际结果：演示收藏集和界面设置保存成功，退出时未发现错误。

5. 再次运行 `npm run start:demo`，检查图库、收藏集和刚才调整的界面设置。
   - 预期结果：14 张演示图片、“本周精选”及界面设置均恢复，Demo Profile 没有变回空库。
   - 实际结果：重新启动后演示图库、收藏集和界面设置均已恢复。

6. 完全退出 Demo Profile，再运行 `npm start` 并进入图片库与设置页。
   - 预期结果：步骤 1 的默认图库和原设置保持不变；14 张演示图片、演示收藏集和 Demo Profile 的界面设置没有出现在默认环境。
   - 实际结果：返回默认环境后原图库与设置保持不变，未出现演示数据。

7. 完全退出默认环境，再运行 `npm run start:demo`。
   - 预期结果：应用重新显示演示图库，证明两个启动入口可以稳定往返且没有串库。
   - 实际结果：再次进入 Demo Profile 后仍显示独立演示图库，两个入口可稳定往返。

8. 完全退出应用，在 `%APPDATA%` 中只读检查目录结构。
   - 预期结果：默认环境与 `NovelAI Prompt Studio-demo` 为两个同级目录；后者独立包含 `data`、`assets`、`session` 和 `workbench-temp`，且默认环境文件未被移动。
   - 实际结果：确认默认环境与 `NovelAI Prompt Studio-demo` 使用独立的同级数据根目录。

## 验证结论

- 总体结果：通过。
- 发现的问题：无。
- 验证人：用户（结果反馈）与 Codex（演示环境取图复核）
- 验证日期：2026-08-23
