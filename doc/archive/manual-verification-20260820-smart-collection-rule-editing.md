# 智能收藏集规则编辑手动验证

## 验证信息

- 变更或提交：`2f965a7 feat: edit smart collection rules`、`d05f3ae fix: remove smart collection edit accent`
- 目标操作系统：Windows 10 / Windows 11
- 前置条件：已安装当前提交对应的应用构建；图库中至少有一个包含搜索或高级筛选条件的智能收藏集；测试图片能够形成多个不同筛选结果，最好包含一个自动图组。

## 验证步骤

1. 打开图片库与收藏集面板，将鼠标移到一个智能收藏集上，点击“编辑规则”图标。
   - 预期结果：应用进入清晰的规则编辑状态；顶部出现包含收藏集名称的编辑提示条；该收藏集在列表中显示“正在编辑规则”，并且左侧没有单边强调色装饰条；原有搜索、Tag、模型、Vibe 和时间条件均正确载入；结果范围来自全部有效图片，而不是先受原收藏集结果限制。
   - 实际结果：通过，与预期一致。

2. 依次修改搜索词和一种或多种高级筛选条件，观察图库卡片、组数和图片数。
   - 预期结果：每次修改都会立即更新预览结果；搜索和所有筛选仍先作用于单张图片，再对真正命中的图片分组；未命中的组内成员不会出现在擦动预览、详情或批量选择范围中。
   - 实际结果：通过，与预期一致。

3. 清除全部搜索和筛选条件。
   - 预期结果：“保存修改”按钮变为不可用，不能保存一个没有任何规则的智能收藏集；“取消”仍可使用。
   - 实际结果：通过，与预期一致。

4. 恢复至少一个条件，继续修改后点击“取消”。
   - 预期结果：应用返回原智能收藏集；未保存的修改被丢弃；再次进入编辑时仍载入取消前的原规则。
   - 实际结果：通过，与预期一致。

5. 再次编辑规则，修改条件后点击“保存修改”。
   - 预期结果：保存成功后自动返回该智能收藏集；收藏集结果使用新规则；旧规则不再生效；收藏集名称和类型保持不变。
   - 实际结果：通过，与预期一致。

6. 重启应用并重新打开刚才修改的智能收藏集。
   - 预期结果：新规则和对应结果在完整重启后仍然保留。
   - 实际结果：通过，与预期一致。

7. 进入规则编辑后切换到“回收站”，再返回“全部”。
   - 预期结果：规则编辑状态安全结束，临时规则不会被保存；回收站继续禁用收藏集入口；返回全部后不会残留编辑提示条。
   - 实际结果：通过，与预期一致。

8. 进入一个智能收藏集的规则编辑状态，然后从收藏集列表删除该收藏集并确认。
   - 预期结果：收藏集被删除，编辑状态与临时筛选一并清理；图片文件和图库图片不受影响；界面不会指向已删除的收藏集。
   - 实际结果：通过，与预期一致。

9. 在浅色、深色主题和较窄窗口下重复进入编辑、修改、保存和取消，并使用键盘访问相关按钮。
   - 预期结果：编辑提示条、收藏集编辑标记、焦点态和禁用态清晰一致；长收藏集名称正确截断；窄窗口会自然换行，不遮挡筛选控件或图库内容。
   - 实际结果：通过，与预期一致。

## 验证结论

- 总体结果：通过。用户于 2026-08-20 完成 Windows 交互验证，智能收藏集规则载入、实时预览、保存、取消、持久化和边界状态均正常。
- 发现的问题：初次验证发现收藏集编辑态左侧存在多余的强调色装饰条；已在 `d05f3ae` 中移除，复验通过。
