# 批次 A 人工取图与宣传素材验收

## 验证对象

- 变更：产品品味与体验改进计划批次 A，基线更新到 v0.9.x，并准备新的演示数据、README 截图与主视觉
- 目标操作系统：Windows 11；最终宣传图可选择一个平台统一取图，macOS 用于确认界面不存在平台特有回归
- 自动检查：干净 `npm ci` 成功；`npm test` 通过 254 项测试；`npm run build` 成功；Lobe UI 5.20.3 审计为 0 个问题
- 本次取图检查：仅变更文档与媒体资源，按 `AGENTS.md` 未重复运行 `npm ci`、`npm test` 或 `npm run build`

## 前置条件

- 使用包含本批次改动的当前分支或提交。
- 阅读并完成 [`promo-demo-data-v090.md`](./promo-demo-data-v090.md) 中的数据准备。
- 使用独立的演示数据目录和应用数据目录，不连接私人图片库。
- 确认所有演示图片及 Prompt 可以公开使用。
- 关闭开发者工具、调试日志和无关应用通知。

## 验证步骤

1. 启动当前稳定版，导入专用演示数据并建立规范中的两个收藏集。
   - 预期结果：图库只包含演示图片；图片名简短可读；没有 UUID、随机哈希、私人路径或日常图库内容。
   - 实际结果：通过。Demo Profile 中仅有 14 张演示图片；已建立“本周精选”（4 张）和“构图研究”（5 张）。

2. 在工作台打开三张演示图片，将主图切换到“按分类 + 原文/译文对照”，并展开能体现 Base 与 Character 差异的内容。
   - 预期结果：三个工作台标签均显示可读名称；主图、分类、双语 Tag 和 Prompt 作用域关系清晰，没有调试 Toast 或遮挡层。
   - 实际结果：通过。工作台打开三张可读名称标签，当前图为“雨后候车亭-02”，使用按分类与原文/译文对照，并显示 Base 与 Character 作用域。

3. 分别在浅色、深色和跟随系统主题下检查工作台，选择层级最清楚的一套主题截取 `workbench-v090.webp` 的原始画面。
   - 预期结果：三种主题均可用；最终截图中的作品是视觉中心，控制区和 Tag 仍能在 README 常见宽度下辨认。
   - 实际结果：最终采用层级最清楚的深色主题，已保存 `workbench-v090.webp`；本次取图未重复执行完整主题切换矩阵。

4. 进入图库，依次准备普通浏览、图片组浏览和多选三个状态，截取 `gallery-v090.webp`、`gallery-groups-v090.webp` 与 `gallery-batch-v090.webp`。
   - 预期结果：收藏集、筛选、图片组、当前成员和批量作用范围均能从画面直接理解；没有截断的随机文件名。
   - 实际结果：通过。已保存普通图库、基础 Prompt 分组和跨系列三图多选三个真实状态。

5. 打开设置，分别准备外观设置和关于/更新状态，截取 `settings-appearance-v090.webp` 与 `settings-updates-v090.webp`。
   - 预期结果：画面不包含 API Key、私人 Base URL、个人目录或调试信息；版本号和平台更新方式与当前构建一致。
   - 实际结果：通过。外观页未显示私人目录；关于与更新页显示开发模式、当前版本 v0.9.2 和官方稳定版本状态。

6. 使用真实的 `workbench-v090.webp` 生成 README 主视觉 `hero-v090.webp`，只编辑应用窗口外围背景。
   - 预期结果：应用内部文字、控件和数据保持真实；外围呈现安静的创作档案台语义，不使用霓虹线路、紫蓝渐变字、玻璃卡片或虚构指标。
   - 实际结果：通过。`hero-v090.webp` 直接嵌入真实工作台截图，只在外围增加低对比度档案台背景与标题。

7. 将七张新图片放入 `doc/screenshots/`，更新 README 中的全部 `v042` 引用，并在 GitHub README 常见宽度下预览。
   - 预期结果：README 不再引用 `v042` 素材；首屏先说明用户问题和核心工作流；缩小后仍能区分工作台、图库和设置。
   - 实际结果：通过。七张 WebP 已放入 `doc/screenshots/`，README 的全部宣传图引用已切换到 `v090`。

8. 在 Windows 与 macOS 各检查一次 README 图片和应用实际界面的对应关系。
   - 预期结果：宣传图没有暗示不存在的平台能力；窗口 chrome 的差异不影响产品内容，文字和快捷键说明符合对应平台。
   - 实际结果：Windows 对应关系已检查通过；macOS 平台对应检查待交接。

## 验证结论

- 总体结果：Windows 宣传取图与 README 素材替换通过；macOS 平台对应检查待完成。
- 发现的问题：无 Windows 阻塞问题。
- 验证人：Codex（用户已授权 Computer Use）
- 验证日期：2026-08-23
