# 已安装版本工作台胶片条高度验证

## 验证对象

- 变更：修复生产构建中 Lobe UI 运行时样式覆盖工作台胶片条高度，导致缩略图标签底部被裁切的问题
- 相关提交：`072e28a`
- 目标操作系统：Windows 11
- 自动检查：`npm test` 通过 41 个测试文件、254 项测试；`npm run build` 成功；Lobe UI 5.20.3 审计为 0 个问题

## 前置条件

- 由验证人自行构建并安装包含提交 `072e28a` 的新版本。
- 准备至少 12 张可在工作台同时打开的图片，其中包含一个有未保存修改的标签。
- 保留一份开发模式画面，便于对比已安装版本与 `npm run dev` 的胶片条几何尺寸。

## 验证步骤

1. 启动已安装版本，在工作台同时打开至少 12 张图片。
   - 预期结果：每个缩略图标签完整显示，胶片条高度稳定；缩略图、序号、关闭按钮和底部边缘均不被裁切。
   - 实际结果：

2. 在开发模式与已安装版本之间对比同一组工作台标签。
   - 预期结果：两种运行方式下的标签均为完整的 58 × 58 像素胶片格，垂直布局没有可见差异。
   - 实际结果：

3. 缩窄窗口直至胶片条溢出，使用前后导航按钮和鼠标滚轮切换到首个、中间及最后一个标签。
   - 预期结果：胶片条保持单行且不出现原生横向滚动条；当前标签能自动进入可视区域，所有标签仍完整显示。
   - 实际结果：

4. 依次检查普通、当前、悬停、未保存修改和显示关闭按钮的标签状态。
   - 预期结果：选中边框、悬停预览、修改标记、序号和关闭按钮都位于胶片格内，不发生上下裁切或重叠。
   - 实际结果：

5. 在浅色、深色主题以及 Windows 100%、125%、150% 显示缩放下重复检查胶片条。
   - 预期结果：主题和显示缩放不会改变标签的完整性，胶片条无底部裁切、异常空隙或纵向抖动。
   - 实际结果：

## 验证结论

- 总体结果：通过。用户确认已安装的新版本不再出现胶片条底部裁切。
- 发现的问题：高度问题已解决；用户另行指出两个胶片格之间的间隔偏宽，作为后续视觉密度调整处理。
- 验证人：用户
- 验证日期：2026-08-23
