# 依赖安全更新手工验证

## 待验证变更

- 提交：`545ea46`（`fix: update vulnerable transitive dependencies`）
- 内容：在不升级顶层依赖的前提下，将 14 个存在安全公告的间接依赖更新到兼容的修复版本；`npm audit` 已从 8 个漏洞降为 0。

## 目标操作系统

Windows 10/11（x64）。

## 前置条件

- 使用提交 `545ea46` 构建的 `release/NovelAI-Prompt-Studio-Setup-0.6.0-x64.exe`。
- 备份重要工作台草稿与图库数据。
- 保持网络可用，以便检查更新服务。

## 验证步骤

1. 运行安装程序并完成安装，然后从开始菜单或桌面快捷方式启动应用。

   预期结果：安装过程正常完成；应用能够启动，不出现白屏、崩溃或缺少模块错误。

   实际结果：通过，与预期一致。

2. 打开一张带 NovelAI 元数据的图片，在工作台切换标签页、筛选 Tag，并悬停查看 Tag 与图片预览。

   预期结果：图片解析、工作台状态、Tag 筛选、悬停预览和标签页切换均正常。

   实际结果：通过，与预期一致。

3. 进入图库并确认已有图片、缩略图和详情能够正常加载。

   预期结果：图库数据保持完整，图片与缩略图正常显示，不出现资源读取错误。

   实际结果：通过，与预期一致。

4. 打开“设置 → 关于与更新”，执行一次检查更新。

   预期结果：更新检查能够完成并正确显示当前版本或可用版本；不出现 YAML 解析、网络响应或更新元数据错误。

   实际结果：通过，与预期一致。

5. 关闭并重新启动应用，确认工作台标签页、筛选状态和设置仍然保留。

   预期结果：应用能够再次启动，持久化状态正常恢复。

   实际结果：通过，与预期一致。

## 总体结果

通过。安装、启动、工作台与图库主要流程、更新检查和重启持久化均正常，依赖审计保持零漏洞。

## 发现的问题

无。

## 验证记录

- 验证人：用户
- 验证日期：2026-08-20
