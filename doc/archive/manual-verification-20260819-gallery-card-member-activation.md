# 图库卡片名称降级与组员激活手动验证

## 验证对象

- 变更提交：`0a24aa1`（移除图库卡片常驻名称与底部信息区；按卡片宽度显示 Hover 文件名；单击详情和双击工作台使用当前擦动组员；固定右栏不再被空白点击收起）
- 目标操作系统：Windows 11；macOS 当前受支持版本
- 自动检查：`npm ci` 成功；`npm test` 通过 167 项测试；`npm run build` 成功；Lobe UI 5.20.3 审计为 0 个问题

## 前置条件

- 图片库中准备至少一张单图和一个包含 4 张以上图片的组图；组内图片应具有容易区分的画面和文件名，并设置一个容易辨认的头图。
- 确认图片库右侧详情栏可以在浮动和固定模式之间切换。
- 工作台中没有会阻碍切页的未处理弹窗。

## 验证步骤

1. 将缩略图调到舒适尺寸，观察单图和组图卡片，再把鼠标移入卡片并移开。
   - 预期结果：卡片不再常驻显示文件名、Tag 数量和尺寸，图片区域成为完整卡片主体；鼠标移入后，底部出现单行文件名，移开后消失，卡片高度和网格位置始终不跳动。
   - 实际结果：

2. 在组图卡片内从左向右擦动，逐段核对卡片底部文件名、悬停预览图片、预览尺寸和 `当前位置 / 总数`。
   - 预期结果：文件名与悬停预览始终指向同一组员，并随横向位置同步更新；名称层不拦截鼠标，也不影响擦动流畅度。
   - 实际结果：

3. 将缩略图逐步缩小并反复悬停，重点检查实际卡片宽度约 172 像素上下以及最小尺寸。
   - 预期结果：宽度足够时显示 Hover 文件名；卡片实际宽度低于约 172 像素后不再显示名称，即使网格列因窗口宽度变化而伸缩也按实际宽度判断。悬停大图预览和组图擦动仍然可用。
   - 实际结果：

4. 在组图中擦动到一个非头图组员后单击卡片，再继续悬停和擦动其他卡片。
   - 预期结果：右侧详情栏打开点击瞬间对应的非头图，标题、图片、尺寸、Tag、日期和组内序号均与该组员一致；后续仅悬停其他图片不会改变已经固定在详情栏中的图片。
   - 实际结果：

5. 分别擦动到组图中的头图和两个非头图后双击，并包含一次右侧详情栏原本收起时的快速双击。
   - 预期结果：每次都在工作台打开双击位置对应的组员，不再强制打开头图；双击过程中的首次单击即使打开详情栏，也不会导致第二次点击改成其他组员。
   - 实际结果：

6. 使用 Tab 键聚焦单图和组图卡片，然后按 Enter，不使用鼠标位置。
   - 预期结果：卡片具有清晰的无障碍名称；宽度足够时聚焦也显示文件名。单图打开自身，组图在没有鼠标位置时使用头图作为键盘默认目标。
   - 实际结果：

7. 将右侧详情栏设为浮动模式并打开一张图片，依次点击网格间隙、网格下方空白、图片卡片、缩略图滑杆和工具栏控件。
   - 预期结果：只有图库网格空白点击会收起浮动详情栏；图片卡片仍打开详情，滑杆和工具栏操作不会被误判为空白点击。
   - 实际结果：

8. 将右侧详情栏固定并打开一张图片，再点击网格间隙和网格下方空白，最后使用明确的收起按钮。
   - 预期结果：固定详情栏不会因任何图库空白点击而收起；明确点击收起按钮仍可收起。固定/浮动和展开/收起偏好在页面切换及应用重启后继续遵循现有持久化规则。
   - 实际结果：

9. 验证组图选择按钮、Ctrl/Shift 多选、右键菜单、设置头图、重命名以及回收站卡片。
   - 预期结果：选择仍以整个图片组为范围；现有组级右键菜单、头图和重命名行为不变；回收站图片仍不能通过双击在工作台打开。
   - 实际结果：

10. 分别在浅色、深色和减少/关闭动效模式下重复步骤 1 至 5，并在最小窗口宽度附近检查网格。
    - 预期结果：文件名在不同图片背景上均清晰可读，长名称单行省略；减少或关闭动效时功能保持即时可用；紧凑网格、组图角标、选择控件和 Hover 文件名没有重叠或裁切。
    - 实际结果：

## 验证结论

- 总体结果：通过（Windows；图片优先卡片、组员激活和固定右栏交互正常）
- 发现的问题：缩略图尺寸滑杆位于最左或最右端时，滑块与两侧图标的视觉间距偏小；由 `a8fd737` 跟进修复。
- 验证人：用户
- 验证日期：2026-08-19
