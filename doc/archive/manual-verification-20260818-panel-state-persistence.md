# 工作台与图片库侧栏状态持久化手动验证

## 验证对象

- 变更提交：`aba2e41`（持久化工作台左栏展开状态、图片库右栏展开状态与固定模式）
- 目标操作系统：Windows 11；macOS 当前受支持版本
- 自动检查：`npm test` 通过 157 项测试；`npm run build` 成功；Lobe UI 5.20.3 审计为 0 个问题

## 前置条件

- 工作台中至少打开一张可正常显示 Prompt 的图片。
- 图片库中至少有两组图片，可以打开右侧预览。
- 应用具备正常写入本地界面设置的权限。

## 验证步骤

1. 进入图片库，打开一张图片的右侧预览，点击图钉将预览面板固定为分栏，然后切换到工作台再返回图片库。
   - 预期结果：右侧预览仍处于固定分栏模式，图钉保持已固定状态，图片网格继续为固定面板让出空间。
   - 实际结果：通过，与预期一致。

2. 保持图片库预览面板固定，完整退出并重新启动应用，再进入图片库。
   - 预期结果：右侧预览仍为固定分栏，不会恢复为默认浮动模式；当前预览可用时，面板内容和图钉状态一致。
   - 实际结果：通过，与预期一致。

3. 取消固定，使图片库预览恢复浮动；先切换页面确认一次，再重启应用确认一次。
   - 预期结果：页面切换和应用重启后都保持浮动模式，不会重新固定；浮动面板不改变图片网格的列数和滚动位置。
   - 实际结果：通过，与预期一致。

4. 在图片库收起右侧预览面板，切换到工作台再返回图片库，然后完整退出并重新启动应用再次进入图片库。
   - 预期结果：两次返回图片库时右侧预览均保持收起，不会自动重新打开；固定或浮动偏好仍独立保留。
   - 实际结果：通过，与预期一致。

5. 在步骤 4 的收起状态下主动点击另一张图片。
   - 预期结果：作为明确的预览操作，右侧面板正常重新打开并显示新图片；此后的展开状态成为新的持久化状态。
   - 实际结果：通过，与预期一致。

6. 进入工作台并收起左侧原图/Vibe 面板，切换到图片库再返回工作台，然后完整退出并重新启动应用再次进入工作台。
   - 预期结果：页面往返和应用重启后，工作台左栏均保持收起，Prompt 编辑区继续使用释放出的空间。
   - 实际结果：通过，与预期一致。

7. 重新展开工作台左栏，切换页面并重启应用。
   - 预期结果：工作台左栏保持展开，图片、文件名和 Vibe 内容正常显示；展开状态成为新的持久化状态。
   - 实际结果：通过，与预期一致。

8. 分别调整工作台左栏和图片库右栏的宽度，并为图片库选择固定或浮动模式；随后交叉执行收起、展开、页面切换和应用重启。
   - 预期结果：两个页面各自独立恢复宽度、展开状态和适用的固定模式，互不覆盖；首次收起和展开时没有空白、裁切或布局跳动。
   - 实际结果：通过，与预期一致。

## 验证结论

- 总体结果：通过。侧栏的展开、收起、宽度与图片库固定/浮动状态在页面切换和完整重启后均正确保持。
- 发现的问题：无。
- 验证人：用户
- 验证日期：2026-08-20
