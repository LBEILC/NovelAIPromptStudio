# 图片库打包态工具栏手动验证

## 待测变更

- 提交：`01a7351`（`fix: constrain packaged gallery sort width`）
- 内容：限制图片库排序下拉框在生产/安装包中的宽度，并保证“组 · 张”统计保持单行。

## 目标操作系统

- Windows 10 或 Windows 11

## 前置条件

- 已检出包含提交 `01a7351` 的代码。
- 已安装项目要求的 Node.js 与 npm。
- 图片库中至少有一组图片，以便显示数量统计。
- 打包前已退出正在运行的 NovelAI Prompt Studio，避免安装包文件被占用。

## 验证步骤

1. 运行 `npm ci`、`npm test`、`npm run build`，再运行 `npm run package` 生成 Windows 安装包。
   - 预期结果：依赖安装、全部测试、生产构建和 Windows 打包均成功完成。
   - 实际结果：

2. 安装并启动本次生成的安装包，进入“图片库”，将窗口保持在默认宽度或更宽。
   - 预期结果：“最近导入”排序下拉框保持紧凑宽度，不会横向占满工具栏剩余空间；搜索框、数量统计和“全选当前结果”均正常显示。
   - 实际结果：

3. 检查图片库右侧的“X 组 · Y 张”统计。
   - 预期结果：统计完整显示在同一行，“组 · 张”不会拆成两行；“全选当前结果”也不会因排序下拉框异常变宽而被挤压。
   - 实际结果：

4. 最大化窗口，再恢复窗口并逐步缩窄至响应式换行发生。
   - 预期结果：排序下拉框始终保持约 130px 的布局宽度；宽窗口下工具栏保持单行，窄窗口下可以按既有响应式规则换行，但数量统计自身仍不拆行，也不出现横向溢出。
   - 实际结果：

5. 分别切换浅色与深色主题，并操作一次排序下拉框。
   - 预期结果：两种主题下宽度一致，下拉菜单可正常打开和选择，焦点、文字与边框显示正常。
   - 实际结果：

6. 运行 `npm run dev` 并检查同一位置。
   - 预期结果：开发态与安装包的排序下拉框宽度和工具栏布局一致。
   - 实际结果：

## 总体结果

## 发现的问题
