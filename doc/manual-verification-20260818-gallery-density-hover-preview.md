# 图片库缩略图密度与悬停预览手工验证

## 验证对象

- 变更提交：`d549f23`（图片库缩略图大小滑杆、三档语义缩放、悬停预览与尺寸持久化）
- 目标操作系统：Windows 11；macOS 当前受支持版本
- 自动检查：`npm ci` 完成；`npm test` 通过 154 项测试；`npm run build` 成功；Lobe UI 审计为 0 个问题

## 前置条件

- 图片库中至少有 20 组图片，包含横图、竖图、方图和至少一个多图片组。
- 可以安全执行普通预览、多选和右键菜单操作；本清单不要求删除图片。
- 分别准备浅色和深色主题，并能在设置中切换动效偏好。

## 验证步骤

1. 启动应用并进入图片库，观察排序控件右侧的缩略图大小滑杆。
   - 预期结果：滑杆两侧显示一小一大的图片图标；工具栏内容对齐正常，不遮挡搜索、排序、图片数量或全选操作。
   - 实际结果：

2. 将滑杆从默认位置缓慢向左拖动到中间区间。
   - 预期结果：缩略图随滑杆连续缩小，同一屏可见图片数量增加；进入紧凑密度后名称隐藏，但尺寸和 Tag 数量仍显示，网格间距同步收紧。
   - 实际结果：

3. 将滑杆继续拖到最左端。
   - 预期结果：图库进入总览密度，卡片文字完全隐藏并形成紧凑图片墙；多图片组仍保留层叠关系和清晰的数量数字，选择标记、当前预览和选中边框没有被裁切。
   - 实际结果：

4. 将滑杆拖到最右端。
   - 预期结果：缩略图放大并恢复舒适密度；图片名称、Tag 数量和尺寸重新显示，长名称保持单行省略，不挤压相邻卡片。
   - 实际结果：

5. 在三种密度下分别将鼠标停留在横图、竖图、方图和多图片组上，并连续移动到相邻卡片。
   - 预期结果：停留约半秒后出现共享悬停预览；图片按原始比例完整显示，不拉伸、不裁切；浮层显示尺寸、Tag 数量、日期和适用的变体数量。快速扫过卡片时不会连续闪烁，移到窗口边缘时浮层会自动避让且不超出可用区域。
   - 实际结果：

6. 在悬停预览出现后点击卡片、点击左上角选择标记并打开右键菜单。
   - 预期结果：点击仍打开右侧详情；多选与右键菜单行为不变；悬停预览不会拦截点击、选择或菜单操作。
   - 实际结果：

7. 使用 Tab 将焦点移动到缩略图大小滑杆，再使用方向键调整数值；随后分别切换浅色、深色以及“减少动态效果/关闭动效”。
   - 预期结果：滑杆具有清晰焦点并可通过键盘调整；读屏可识别“缩略图大小”和像素值；两种主题下控件与浮层颜色正确；减少或关闭动效后功能仍可用，不出现明显空间动画。
   - 实际结果：

8. 把缩略图设置为容易辨认的非默认大小，切换到工作台再返回图片库，然后完整退出并重新启动应用。
   - 预期结果：页面切换和重启后都恢复之前选择的缩略图大小与对应密度，不恢复默认值。
   - 实际结果：

9. 将应用窗口缩窄到允许的最小宽度，再分别固定和取消固定右侧图片预览面板。
   - 预期结果：工具栏自然换行，滑杆不会被压缩到无法操作；网格至少保持一列且不产生水平溢出；固定预览面板时缩略图密度仍能调整。
   - 实际结果：

## 验证结论

- 总体结果：
- 发现的问题：
- 验证人：
- 验证日期：
