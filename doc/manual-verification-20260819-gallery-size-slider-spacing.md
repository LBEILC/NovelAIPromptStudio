# 图库缩略图尺寸滑杆端点间距手动验证

## 验证对象

- 变更提交：`a8fd737`（增加图库缩略图尺寸滑杆与两侧图标的端点留白）
- 目标操作系统：Windows 11；macOS 当前受支持版本
- 自动检查：`npm ci` 成功；`npm test` 通过 167 项测试；`npm run build` 成功；Lobe UI 5.20.3 审计为 0 个问题

## 前置条件

- 打开图片库，并确保顶部工具栏中的缩略图尺寸滑杆可见。
- 图片库中存在足够多的图片，以便观察缩略图尺寸变化。

## 验证步骤

1. 将滑杆拖到最左端、中央和最右端，分别观察滑块与两侧图片图标的距离。
   - 预期结果：滑块位于任一端点时都与相邻图标保留清晰、对称的视觉间距，不再近似贴合；轨道长度、最小值和最大值没有变化。
   - 实际结果：

2. 使用 Tab 聚焦滑杆，并通过方向键、Home 和 End 到达两个端点。
   - 预期结果：键盘操作正常，焦点轮廓不与两侧图标重叠，辅助文本仍准确报告缩略图像素值。
   - 实际结果：

3. 将窗口缩到工具栏开始换行的宽度，并在浅色、深色主题下重复步骤 1。
   - 预期结果：尺寸控制保持为一个完整分组，工具栏可以正常换行；滑杆、图标、搜索框和计数均无重叠或裁切。
   - 实际结果：

4. 切换到工作台再返回图片库，并重启应用。
   - 预期结果：间距保持不变，缩略图尺寸值继续按原逻辑持久化。
   - 实际结果：

## 验证结论

- 总体结果：
- 发现的问题：
- 验证人：
- 验证日期：
