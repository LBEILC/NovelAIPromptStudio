# 设置页反馈与支持人工验证

## 验证对象

- 变更：主线暂时隐藏尚未完成的帮助中心，将设置页入口收敛为“反馈与支持”，保留诊断信息复制、报告问题和提出建议。
- 相关提交：`f09fc39`
- 完整帮助中心保留分支：`codex/help-center-polish`（创建点 `6445b43`）
- 目标操作系统：Windows 11、macOS
- 自动检查：独立工作区 `npm ci` 成功；`npm test` 通过 50 个测试文件、286 项测试；`npm run build` 成功；Lobe UI 5.20.3 审计为 0 个问题。

## 前置条件

- 使用包含提交 `f09fc39` 的开发版本或安装包。
- 网络可用，以便验证官方 GitHub 反馈入口。
- 测试期间不要实际提交问题或建议，除非确实需要反馈。

## 验证步骤

1. 打开设置页，检查左侧“支持”分组。
   - 预期结果：“帮助与反馈”已替换为“反馈与支持”；“关于与更新”继续显示；设置页重新设计后的分组、对齐、选中表面和其他入口均保持不变；主线中没有可进入的帮助文章菜单。
   - 实际结果：

2. 进入“反馈与支持”，观察页面内容。
   - 预期结果：页面只显示反馈用途说明以及“复制诊断信息”“报告问题”“提出建议”三个按钮；不显示帮助搜索、文章分组、文章数量或功能演示；页面没有空白的帮助导航占位。
   - 实际结果：

3. 点击“复制诊断信息”，检查剪贴板内容。
   - 预期结果：出现复制成功提示；剪贴板只包含 `NovelAI Prompt Studio v<版本>` 和当前系统平台，不包含图片、Prompt、文件路径、API 地址、API Key 或日志。
   - 实际结果：

4. 分别点击“报告问题”和“提出建议”。
   - 预期结果：系统默认浏览器只打开官方 `LBEILC/NovelAIPromptStudio` GitHub 新问题页面，分别预填问题和建议标题；应用不会自动上传或提交任何信息。
   - 实际结果：

5. 在浅色、深色和跟随系统主题下检查宽窗口与窄窗口，并使用键盘遍历入口和三个按钮。
   - 预期结果：反馈卡片保持设置页现有视觉语言；窄窗口下操作按钮自然换行且没有裁切；焦点清晰；返回其他设置入口后，外观、资源库、AI 服务、Tag 数据和关于与更新功能不受影响。
   - 实际结果：

## 验证结论

- 总体结果：
- 发现的问题：
- 验证人：
- 验证日期：
