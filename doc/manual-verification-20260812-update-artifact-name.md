# Windows 自动更新产物名称验证

## 测试对象

- 变更：Windows 安装包改用固定的安全文件名，并在发布前校验 `latest.yml` 引用的产物均已生成。
- 目标系统：Windows 10/11 x64。

## 前置条件

- 准备一个包含本次变更的新版 GitHub Release；Release 中的 `latest.yml`、`.exe` 和 `.exe.blockmap` 已上传。
- 已安装一个版本号低于该 Release 的 NovelAI Prompt Studio。
- 已备份重要的本地图片库和设置。

## 验证步骤

1. 打开新版 GitHub Release 的资产列表，对照 `latest.yml` 中的 `url` 和 `path`。
   - 预期结果：两处均为 `NovelAI-Prompt-Studio-Setup-<版本>-x64.exe`，Release 中存在完全同名的 EXE 和对应的 `.exe.blockmap`。
   - 实际结果：

2. 启动已安装的旧版应用，进入“设置”，点击“检查更新”。
   - 预期结果：应用识别到新版，显示正确的版本号和“下载更新”操作，不出现检查更新错误。
   - 实际结果：

3. 点击“下载更新”，等待进度完成。
   - 预期结果：下载进度正常推进到 100%，不再出现 `Cannot download` 或 HTTP 404 提示，并出现“重启并安装”。
   - 实际结果：

4. 点击“重启并安装”，等待应用重新启动，再次打开“设置”。
   - 预期结果：安装完成，当前版本号更新为新版版本号，检查更新显示已是最新版本。
   - 实际结果：

5. 打开更新前已有的图片和设置，并执行一次普通的本地编辑与保存。
   - 预期结果：本地数据和设置完整，图片可正常打开，编辑与保存功能不受更新影响。
   - 实际结果：

## 验证结论

- 总体结果：
- 发现的问题：
