# 悬停预览穿透与图库快捷预览手动验证

## 验证对象

- 变更提交：`d7f8f98`（Tag 与工作区标签页悬停预览不参与鼠标命中；图库双击图片在工作台打开）；`902a217`（修正 Tag 共享定位层的鼠标穿透；图库空白点击收栏；组图横向擦动预览）；`1f1ccf4`（移除组图擦动对共享 Popover 首次打开回调的依赖）；`189d05b`（取消头图初始态和移动门槛，进入组图时直接按鼠标位置预览）
- 目标操作系统：Windows 11；macOS 当前受支持版本
- 自动检查：`npm ci` 成功；`npm test` 通过 164 项测试；`npm run build` 成功；Lobe UI 5.20.3 审计为 0 个问题

## 前置条件

- 工作台中至少打开 4 个图片标签页，并在 Prompt 区域显示足够多的 Tag，使悬停预览可能覆盖相邻 Tag 或标签页。
- 图片库中准备至少一张单图和至少三个多图片组；多图片组具有容易辨认的当前头图。
- 回收站中准备一张可安全用于验证的测试图片。

## 验证步骤

1. 在工作台中将鼠标停留在一个 Tag 上，等待悬停预览出现，再直接移向被预览浮层视觉覆盖的相邻 Tag，并连续经过多枚 Tag。
   - 预期结果：悬停预览不参与鼠标命中；下方 Tag 能立即响应，预览内容连续切换，不需要绕开浮层，也不会出现明显闪烁或停顿。
   - 首次实际结果：`d7f8f98` 未通过；单个 Tooltip 的 Popup 已穿透，但共享 TooltipGroup 的外层 Positioner 仍然阻挡鼠标。
   - `902a217` 修复后复测结果：

2. 在 Tag 悬停预览出现后单击 Tag，并分别测试右键菜单和拖动排序。
   - 预期结果：单击仍打开可交互的 Tag 编辑浮层；右键菜单与拖动排序保持原有行为。只有悬停说明层穿透鼠标，点击编辑层本身仍可操作。
   - 实际结果：

3. 将鼠标停留在一个工作区标签页上，等待图片预览出现，再直接移向被预览浮层视觉覆盖的其他标签页，并连续经过多个标签页。
   - 预期结果：下方标签页立即响应并切换对应悬停预览；不需要绕开图片预览，标签切换、关闭按钮和拖动排序仍然正常。
   - 实际结果：

4. 在图片库中双击一张普通图片卡片。
   - 预期结果：应用切换到工作台，并通过现有打开流程创建或复用对应图片标签页；单击预览、多选标记和右键菜单行为没有改变。
   - 实际结果：

5. 在图片库中为一个多图片组设置容易辨认的头图，然后双击该组卡片。
   - 预期结果：工作台打开该组当前头图，而不是组内其他图片；图片来源仍标记为图库资源。
   - 实际结果：

6. 返回图片库的回收站视图并双击测试图片。
   - 预期结果：不会在工作台打开回收站图片，保持现有“先恢复再编辑”的产品边界；恢复和永久删除操作不受影响。
   - 实际结果：

7. 打开图片库右侧预览，分别在浮动和固定模式下点击图片卡片之间、图片行末尾和网格下方的空白区域。
   - 预期结果：点击空白处立即收起右侧预览栏，并保存收起状态；点击图片卡片本身不会被误判为空白点击，仍按原逻辑打开预览。
   - 实际结果：

8. 分别从一个多图片组卡片的左侧、中部和右侧移入鼠标，等待悬停预览出现；保持鼠标位于原卡片内，从左向右缓慢移动，再反向移动。
   - 预期结果：预览首次出现的图片直接对应鼠标进入时所在的横向分区，不强制先展示头图，也不需要额外移动达到门槛。之后横向位置持续映射到组内图片，预览图片、尺寸、Tag 数量、日期、`当前位置 / 总数` 和细进度条同步更新。图片不会自动播放；鼠标离开再从其他位置进入时，按新的进入位置重新计算。
   - `902a217` 首次实际结果：工作台切回图片库后，每个组图首次悬停只能显示头图；离开并第二次进入同一组图后，擦动才恢复。
   - `1f1ccf4` 调整结果：不再依赖共享 Popover 首次打开回调，但仍保留先显示头图和约 8 像素的移动门槛。
   - `189d05b` 调整后复测结果：

9. 切换到工作台再返回图片库，依次从不同横向位置首次进入至少三个不同组图；每次等待预览出现后，在不离开卡片的情况下横向移动鼠标。
   - 预期结果：页面切换后访问的每个组图都在第一次悬停时直接显示进入位置对应的组员，不需要先显示头图、额外移动或移开后第二次进入；继续横向移动时预览立即跟随。
   - 实际结果：

10. 在步骤 8 擦动到非头图后直接双击组卡片，再对包含较多图片的组快速横向擦动。
   - 预期结果：双击始终在工作台打开当前头图，不受最后擦动位置影响；快速擦动只在跨过成员区间时更新，不出现明显卡顿，预览浮层仍不阻挡其他图片卡片。
   - 实际结果：

11. 分别在浅色、深色以及减少/关闭动效模式下快速重复步骤 1、3、4、7、8、9 和 10。
   - 预期结果：两种主题下预览显示正确；减少或关闭动效时功能保持可用；所有视觉预览都不会在出现或切换期间阻断鼠标操作。
   - 实际结果：

## 验证结论

- 总体结果：
- 发现的问题：
- 验证人：
- 验证日期：
