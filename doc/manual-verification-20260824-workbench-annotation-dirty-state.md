# 工作台翻译与分类不触发“已修改”验证

## 验证信息

- 待验证变更：`7e3a5e8 fix: keep tag annotations out of prompt dirty state`
- 目标操作系统：Windows 11；macOS 需按 `coordination/MACOS.md` 完成对应验证
- 验证方式：人工交互验证

## 前置条件

1. 使用包含可解析 Prompt 的 NovelAI 图片，且 Prompt 中至少有一个可执行翻译或分类的 Tag。
2. 图片尚未在工作台中进行真实 Prompt 编辑；打开后不应显示“已修改”标记。
3. 如需确认重启行为，先确保其他重要草稿已经保存或关闭。

## 验证步骤

### 1. 确认初始状态

1. 从图库打开测试图片到工作台。
2. 观察当前缩略图标签及关闭图片时的行为。

- 预期结果：图片不显示“已修改”标记；关闭时不会出现丢弃 Prompt 修改的确认框。
- 实际结果：

### 2. 执行批量“翻译与分类”

1. 再次打开测试图片。
2. 点击工作台中的“翻译与分类”操作并等待完成。
3. 确认 Tag 的翻译、分类或来源信息已经更新。
4. 观察工作台缩略图标签。

- 预期结果：翻译与分类结果正常显示，但图片始终不出现“已修改”标记。
- 实际结果：

### 3. 验证单个 Tag 的注释操作

1. 对单个 Tag 使用翻译或分类相关操作。
2. 如界面允许，手动调整该 Tag 的翻译或分类。
3. 在原文、译文和双语显示模式之间切换，并切换分类筛选。

- 预期结果：翻译、分类、来源和备注等注释变化不会触发“已修改”；显示模式和筛选切换也不会触发“已修改”。
- 实际结果：

### 4. 验证关闭与重启边界

1. 保持仅发生翻译或分类变化的状态，关闭当前工作台图片。
2. 确认没有出现丢弃 Prompt 修改的确认框。
3. 重新打开图片，记录原始 Prompt 文本。
4. 完全退出软件并重新启动，再次打开或检查恢复的图片。
5. 复制完整 Prompt，与重启前记录的原始 Prompt 对比。

- 预期结果：关闭时不要求丢弃修改；重启后不显示“已修改”；翻译与分类信息仍可正常使用；原始 Prompt 的空格、换行、括号及 Tag 顺序没有被重写。
- 实际结果：

### 5. 验证真实 Prompt 修改仍会被识别

1. 修改任一 Tag 的文本或权重，或者执行新增、删除、拖动排序等真实 Prompt 编辑。
2. 观察“已修改”标记并尝试关闭图片。
3. 取消关闭，然后使用重置功能恢复原始 Prompt。

- 预期结果：真实 Prompt 编辑立即显示“已修改”，关闭时出现丢弃确认；重置后标记消失，且已有翻译与分类注释仍可正常显示。
- 实际结果：

## 总体结果

- 结果：
- 验证人：
- 验证时间：

## 发现的问题


