# 工作台标签页紧凑自适应预览手工验证

## 待验证变更

保持标签页预览按图片比例自适应和完整显示，将最大宽度由 360px 调整为 320px、最大高度由 400px 调整为 360px，整体缩小约 10%。

## 目标操作系统

Windows 10/11。

## 前置条件

- 准备至少一张常规竖图和一张横图。
- 使用 `npm run dev` 启动应用，并在工作台中分别打开这些图片。

## 验证步骤

1. 依次悬停竖图、横图和方图标签。

   预期结果：预览比上一版更紧凑；竖图仍然变窄、变高，横图仍然变宽、变矮；所有图片完整显示、不裁切、不拉伸。

   实际结果：

2. 在多个标签间移动鼠标，并观察预览与工作台内容的面积关系。

   预期结果：预览能够辨认图片内容，但不会过度遮挡工作台；浮层切换和关闭正常。

   实际结果：

3. 缩窄应用窗口，并分别在浅色、深色主题下检查预览。

   预期结果：预览不溢出窗口，文字保持可读，灰色补充区域与主题协调。

   实际结果：

## 总体结果

待验证。

## 发现的问题

无。
