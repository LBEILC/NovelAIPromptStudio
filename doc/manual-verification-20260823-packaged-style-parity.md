# 开发版与安装版样式一致性验证

## 验证对象

- 变更：消除 Lobe UI 运行时样式晚于应用静态 CSS 注入时造成的开发版与安装版视觉差异
- 相关提交：`ebcd92d`
- 目标操作系统：Windows 11
- 自动检查：`npm test` 通过 42 个测试文件、255 项测试；`npm run build` 成功；Lobe UI 5.20.3 审计为 0 个问题

## 前置条件

- 由验证人自行构建并安装包含提交 `ebcd92d` 的新版本。
- 同时保留 `npm run dev` 开发版，使用相同窗口尺寸、主题和应用数据进行并排比较。
- 准备一个没有 Character Prompt 的项目，以显示“暂无角色 Prompt”操作区。

## 验证步骤

1. 在开发版和安装版中打开没有 Character Prompt 的项目。
   - 预期结果：两边的“暂无角色 Prompt”标题和说明均使用相同的弱化文字色；图标与文字间距均为 12 像素，内边距、虚线边框、圆角和整体高度一致。
   - 实际结果：

2. 分别悬停、键盘聚焦并点击“暂无角色 Prompt”操作区。
   - 预期结果：开发版与安装版的悬停、聚焦和按下反馈一致，修复没有覆盖 Lobe UI 原有交互状态。
   - 实际结果：

3. 对比工作台左侧图片面板和设置页左侧导航的内容边距。
   - 预期结果：工作台图片面板保持随窗口变化的 18–32 像素内边距；设置导航保持上 32、左右 14、下 18 像素，两种运行方式布局一致。
   - 实际结果：

4. 在宽窗口和窄窗口中对比设置页的两个字体选择器。
   - 预期结果：宽窗口下选择器宽度均为 260 像素；窄窗口下均扩展到可用宽度，开发版与安装版一致。
   - 实际结果：

5. 对比图库工具栏中的缩略图尺寸滑杆。
   - 预期结果：滑杆宽度均为 96 像素，与两侧图标的间距和工具栏换行行为一致。
   - 实际结果：

6. 在浅色和深色主题下重复以上对比。
   - 预期结果：颜色、间距、边框、圆角、宽度和面板内边距均无开发版/安装版差异。
   - 实际结果：

## 验证结论

- 总体结果：
- 发现的问题：
- 验证人：
- 验证日期：
