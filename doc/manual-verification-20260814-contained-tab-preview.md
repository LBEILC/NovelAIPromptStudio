# 工作台标签页完整图片预览手工验证

## 待验证变更

修正工作台标签页悬停预览中的图片尺寸约束：图片在固定正方形灰色画布内居中完整显示，保持原始比例，不裁切、不拉伸，剩余区域由灰色背景填充。

## 目标操作系统

Windows 10/11。

## 前置条件

- 准备至少一张明显的竖图和一张横图。
- 使用 `npm run dev` 启动应用，并在工作台中分别打开两张图片。

## 验证步骤

1. 将鼠标停留在竖图标签上约半秒。

   预期结果：竖图从顶部到底部完整显示，没有任何内容被裁切；图片保持原始比例并水平居中，两侧空余区域显示为灰色背景。

   实际结果：

2. 将鼠标移到横图标签并停留约半秒。

   预期结果：横图从左到右完整显示，没有任何内容被裁切；图片保持原始比例并垂直居中，上下空余区域显示为灰色背景。

   实际结果：

3. 缩窄应用窗口，并分别在浅色、深色主题下重复查看预览。

   预期结果：预览卡片不溢出窗口；图片仍完整显示；空余背景和文字在两种主题下均清晰协调。

   实际结果：

## 总体结果

待验证。

## 发现的问题

无。
