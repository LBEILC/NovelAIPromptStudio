# 工作台重启后 Prompt 修改状态手动验证

## 验证对象

- 变更提交：`89b09c8`（修复未编辑图片在应用重启后被误判为“已修改”）
- 目标操作系统：Windows 11；macOS 当前受支持版本
- 自动检查：`npm ci` 成功；`npm test` 通过 251 项测试；`npm run build` 成功

## 前置条件

- 使用包含提交 `89b09c8` 的开发版或打包版本。
- 准备至少三张可正常读取 NovelAI Prompt 的图片，其中至少一张包含 Character Prompt。
- 确认这些测试图片的 Prompt 在本轮验证开始前不需要保留工作台草稿；图片文件本身不会被修改。

## 验证步骤

1. 启动应用，在工作台中打开三张测试图片，不编辑任何 Tag、角色名称或 Prompt 内容。
   - 预期结果：三张图片均不显示蓝色修改圆点，工作台标题与图片摘要中均不出现“已修改”。
   - 实际结果：

2. 完整退出应用，然后重新启动，等待三张工作台图片全部恢复。
   - 预期结果：三张图片及当前活动标签正常恢复；所有未编辑图片仍不显示修改圆点或“已修改”文字。
   - 实际结果：

3. 只编辑其中一张图片的任意 Tag 权重、文本、翻译或分类，随后切换查看另外两张图片。
   - 预期结果：只有被编辑的图片显示修改圆点和“已修改”；另外两张图片继续保持未修改状态。
   - 实际结果：

4. 再次完整退出并重新启动应用，等待工作台会话恢复。
   - 预期结果：被编辑图片的草稿和“已修改”状态均被保留；另外两张未编辑图片仍不会被误标为已修改。
   - 实际结果：

5. 对被编辑图片执行“恢复图片中的 Prompt”，确认恢复后再次重启应用。
   - 预期结果：恢复操作后该图片的修改标记立即消失；重启后所有测试图片继续保持未修改状态。
   - 实际结果：

6. 分别尝试关闭一个未修改标签和一个已修改标签。
   - 预期结果：关闭未修改标签不会弹出丢弃草稿确认；关闭已修改标签仍会显示“关闭已修改的标签？”确认，取消后标签与草稿保持不变。
   - 实际结果：

## 验证结论

- 总体结果：
- 发现的问题：
- 验证人：
- 验证日期：
